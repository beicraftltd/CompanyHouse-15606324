{
  "name": "companies-house-lead-test-engineer",
  "version": "1.0.0",
  "description": "Playwright TypeScript test suite for https://automationintesting.online/",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.5.4",
    "eslint": "^9.9.0",
    "prettier": "^3.3.3"
  }
}
